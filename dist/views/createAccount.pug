<!DOCTYPE html>
html(lang="en")
    head
        include partials/head
    body.login
        .content-form
            h1 Crear Cuenta

            if locals.message
                each category in Object.keys(locals.message)
                    each error in message[category]
                        .alert(class=`alert-${category} mx-auto wm-650 w-90`) #{error}
            
            form#form.box.box-login(action="/create-account", method="POST")
                .form-group
                    label(for="") Correo
                    input.form-control(type="email", name="email" value= email placeholder="Ingresa correo" required)
                .form-group
                    label(for="") Contraseña
                    input.form-control(type="password" name="password" value= password placeholder="Contraseña" required)
                .send
                    input(type="submit", value="Crear Cuenta" class="btn btn-blue-mowin")
                .actions
                    a(href="/log-in") Volver a Iniciar Sesión